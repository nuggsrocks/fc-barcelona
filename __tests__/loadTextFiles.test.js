import { loadTextFiles } from '../src/js/loadTextFiles'

import txt1 from '../src/text/history.txt'

describe('loadTextFiles()', () => {
  it('should load all files in given array asynchronously', () => {
    loadTextFiles()
  })
})
